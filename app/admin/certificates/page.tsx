"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { CertificateDocument, printCertificate } from "@/components/certificate-document";

type CertificateType = "project" | "employee" | "building";
type IssuableType = "project" | "employee";
type CertificateKind = "load_bearing" | "construction_supervision" | "construction_completion" | "experience" | "salary";

type CertificateRecord = {
  certificateId: string; verificationUrl?: string; qrUrl?: string; issuedAt: string; type: CertificateType;
  name: string; address: string; position: string; subject: string; reference: string; description: string;
  fatherName?: string; motherName?: string; nidNo?: string; expiresAt?: string; status?: string; revision?: number;
  parentId?: string; supersededBy?: string; revokedAt?: string; revokedReason?: string; deletedAt?: string; deletedReason?: string;
};

type KindConfig = { type: IssuableType; label: string; subject: string; position: string; statement: string };

const kinds: Record<CertificateKind, KindConfig> = {
  load_bearing: {
    type: "project",
    label: "Load Bearing Certificate",
    subject: "Load Bearing & Construction Works Certificate",
    position: "Owner / Client",
    statement: "The structural design and construction works described in this certificate have been reviewed for the stated project scope."
  },
  construction_supervision: {
    type: "project",
    label: "Construction Supervision Certificate",
    subject: "Construction Supervision Certificate",
    position: "Owner / Client",
    statement: "This is to certify that LAND VIEW — Engineering & Architectural Consultancy provided construction supervision for the project described above in accordance with the agreed professional scope and site records."
  },
  construction_completion: {
    type: "project",
    label: "Construction Completion Certificate",
    subject: "Construction Completion Certificate",
    position: "Owner / Client",
    statement: "This is to certify that the construction work for the project described above has been completed to the extent stated in this certificate, based on LAND VIEW — Engineering & Architectural Consultancy project and supervision records."
  },
  experience: {
    type: "employee",
    label: "Experience Certificate",
    subject: "Experience Certificate",
    position: "Employee",
    statement: "During the tenure, the employee has been primarily responsible for site management, construction supervision, coordination, and day-to-day monitoring across structural and architectural projects. Major duties and responsibilities included:\n• Managing day-to-day site activities and monitoring the progress of construction work.\n• Supervising construction materials, concrete work, reinforcement layouts, and other site operations.\n• Coordinating with contractors, masons, laboratory technicians, suppliers, and project personnel.\n• Maintaining daily site records, progress reports, work schedules, and quality-control documentation.\n\nThroughout the service period, the employee’s professional conduct, site management capability, coordination skills, problem-solving ability, and commitment to project quality have been highly commendable. The employee has demonstrated dedication, responsibility, and technical capability.\n\nWe wish continued success."
  },
  salary: {
    type: "employee",
    label: "Salary Certificate",
    subject: "Salary Certificate",
    position: "Employee",
    statement: "This is to certify that the employee named above is / was employed with LAND VIEW — Engineering & Architectural Consultancy in the stated designation. This certificate is issued upon request for official salary / employment confirmation purposes."
  }
};

const kindOrder: Record<IssuableType, CertificateKind[]> = {
  project: ["load_bearing", "construction_supervision", "construction_completion"],
  employee: ["experience", "salary"]
};

const legacyMeta: Record<CertificateType, { label: string; position: string; statement: string }> = {
  project: { label: "Project Certificate", position: "Owner / Client", statement: kinds.load_bearing.statement },
  employee: { label: "Employee Certificate", position: "Employee", statement: kinds.experience.statement },
  building: { label: "Building Certificate", position: "Owner / Representative", statement: "This is to certify that the building / property described above has been reviewed or documented by LAND VIEW — Engineering & Architectural Consultancy for the purpose stated in this certificate." }
};

function localDate(value?: string) { if (!value) return ""; const d = new Date(value); if (Number.isNaN(d.getTime())) return ""; const local = new Date(d.getTime() - d.getTimezoneOffset() * 60000); return local.toISOString().slice(0, 10); }
function today() { return localDate(new Date().toISOString()); }
function displayDate(value?: string) { if (!value) return "—"; const d = new Date(value); return Number.isNaN(d.getTime()) ? value : d.toLocaleDateString("en-GB", { day: "2-digit", month: "long", year: "numeric" }); }
function ordinalDate(value: string) { if (!value) return ""; const [year, month, day] = value.split("-").map(Number); const d = new Date(Date.UTC(year, month - 1, day)); if (Number.isNaN(d.getTime())) return value; const n = d.getUTCDate(); const mod100 = n % 100; const suffix = mod100 >= 11 && mod100 <= 13 ? "th" : n % 10 === 1 ? "st" : n % 10 === 2 ? "nd" : n % 10 === 3 ? "rd" : "th"; return `${n}${suffix} ${d.toLocaleDateString("en-GB", { month: "long", year: "numeric", timeZone: "UTC" })}`; }
function stripServicePeriod(value: string) { return value.replace(/^Service Period:\s*[^\n]+\n*/i, "").trimStart(); }
function serviceDatesFromDescription(value: string) { const match = value.match(/^Service Period:\s*(\d{1,2})(?:st|nd|rd|th)\s+([A-Za-z]+)\s+(\d{4})\s+to\s+(\d{1,2})(?:st|nd|rd|th)\s+([A-Za-z]+)\s+(\d{4})/i); if (!match) return ["", ""] as const; const months = ["january","february","march","april","may","june","july","august","september","october","november","december"]; const iso = (day: string, month: string, year: string) => `${year}-${String(months.indexOf(month.toLowerCase()) + 1).padStart(2,"0")}-${String(Number(day)).padStart(2,"0")}`; return [iso(match[1], match[2], match[3]), iso(match[4], match[5], match[6])] as const; }
function kindFromSubject(subject: string, type: CertificateType): CertificateKind { const s = (subject || "").toLowerCase(); if (s.includes("load bearing")) return "load_bearing"; if (s.includes("construction supervision")) return "construction_supervision"; if (s.includes("construction completion")) return "construction_completion"; if (s.includes("salary")) return "salary"; return type === "employee" ? "experience" : "load_bearing"; }

function projectCertificateStatement(kind: CertificateKind, values: { name: string; buildingStories: string; buildingUse: string; saDag: string; bsDag: string; jlNo: string; mouza: string; landArea: string; ps: string; district: string; structuralDesigner: string; soilTestCompany: string; soilTestDate: string; constructionProgress: string; }) {
  const owner = values.name.trim();
  const stories = values.buildingStories.trim();
  const use = values.buildingUse.trim();
  const location = `Dag No.: S.A. – ${values.saDag.trim()}, B.S. – ${values.bsDag.trim()}; J.L. No. – ${values.jlNo.trim()}, Mouza – ${values.mouza.trim()}; Area of Land – ${values.landArea.trim()} decimal; P.S. – ${values.ps.trim()}, District – ${values.district.trim()}`;

  if (kind === "load_bearing") {
    return `This is to certify that the structural design of the proposed ${stories} Storied ${use} Building of ${owner} at ${location} has been prepared by ${values.structuralDesigner.trim()}. The design of the foundation, columns, grade beams, roof beams, roof slabs, stairs and other structural members has been reviewed and checked by the undersigned and is considered adequate to bear the design loads of the proposed building, subject to construction in accordance with the approved drawings, specifications and design assumptions.\n\nThe foundation design has been prepared considering the soil test report provided by ${values.soilTestCompany.trim()}, dated ${values.soilTestDate.trim()}. The structural design has also been prepared considering the applicable earthquake loads and provisions of the Bangladesh National Building Code (BNBC).\n\nThe construction works up to ${values.constructionProgress.trim()} have been carried out under direct supervision in accordance with the approved drawings and design. Further construction works of this building shall continue under professional supervision.`;
  }

  const scope = kind === "construction_supervision"
    ? "Construction supervision has been provided for the project in accordance with the approved drawings, structural design, applicable specifications and site records."
    : "The construction work for the project has been completed to the extent stated in this certificate in accordance with the approved drawings, structural design, applicable specifications and available supervision records.";
  return `This is to certify that the proposed ${stories} Storied ${use} Building of ${owner} at ${location} was structurally designed by ${values.structuralDesigner.trim()}. The foundation design considered the soil test report provided by ${values.soilTestCompany.trim()}, dated ${values.soilTestDate.trim()}, and the structural design considered the applicable earthquake provisions of the Bangladesh National Building Code (BNBC).\n\n${scope}\n\nRecorded construction stage: ${values.constructionProgress.trim()}.`;
}

export default function CertificatesPage() {
  const [type, setType] = useState<IssuableType>("project");
  const [kind, setKind] = useState<CertificateKind>("load_bearing");
  const [records, setRecords] = useState<CertificateRecord[]>([]);
  const [registryError, setRegistryError] = useState(""); const [loading, setLoading] = useState(false); const [loadingRegistry, setLoadingRegistry] = useState(true);
  const [issued, setIssued] = useState<CertificateRecord | null>(null); const [reissueOf, setReissueOf] = useState("");
  const [name, setName] = useState(""); const [address, setAddress] = useState(""); const [fatherName, setFatherName] = useState(""); const [motherName, setMotherName] = useState(""); const [nidNo, setNidNo] = useState("");
  const [serviceFrom, setServiceFrom] = useState(""); const [serviceTo, setServiceTo] = useState("");
  const [buildingStories, setBuildingStories] = useState("07 (Seven)"); const [buildingUse, setBuildingUse] = useState("Residential");
  const [saDag, setSaDag] = useState(""); const [bsDag, setBsDag] = useState(""); const [jlNo, setJlNo] = useState(""); const [mouza, setMouza] = useState("");
  const [landArea, setLandArea] = useState(""); const [ps, setPs] = useState(""); const [district, setDistrict] = useState("Feni");
  const [structuralDesigner, setStructuralDesigner] = useState(""); const [soilTestCompany, setSoilTestCompany] = useState(""); const [soilTestDate, setSoilTestDate] = useState("");
  const [constructionProgress, setConstructionProgress] = useState("ground floor casting including the foundation");
  const [position, setPosition] = useState(kinds.load_bearing.position); const [subject, setSubject] = useState(kinds.load_bearing.subject); const [reference, setReference] = useState(""); const [description, setDescription] = useState(kinds.load_bearing.statement);
  const [issueDate, setIssueDate] = useState(today()); const [expiryDate, setExpiryDate] = useState(""); const [error, setError] = useState(""); const [query, setQuery] = useState("");

  async function loadRegistry() { setLoadingRegistry(true); setRegistryError(""); try { const response = await fetch("/api/certificates", { cache: "no-store", credentials: "same-origin" }); const json = await response.json(); if (!response.ok || !json?.success) throw new Error(json?.error || "Could not load certificate registry."); setRecords(Array.isArray(json?.data?.certificates) ? json.data.certificates : []); } catch (e: any) { setRegistryError(e?.message || "Could not load certificate registry."); } finally { setLoadingRegistry(false); } }
  useEffect(() => { void loadRegistry(); }, []);

  function selectKind(nextKind: CertificateKind, clearIdentity = false) { const cfg = kinds[nextKind]; setType(cfg.type); setKind(nextKind); setPosition(cfg.position); setSubject(cfg.subject); setDescription(cfg.statement); setServiceFrom(""); setServiceTo(""); if (clearIdentity) { setName(""); setAddress(""); setFatherName(""); setMotherName(""); setNidNo(""); setReference(""); setBuildingStories("07 (Seven)"); setBuildingUse("Residential"); setSaDag(""); setBsDag(""); setJlNo(""); setMouza(""); setLandArea(""); setPs(""); setDistrict("Feni"); setStructuralDesigner(""); setSoilTestCompany(""); setSoilTestDate(""); setConstructionProgress("ground floor casting including the foundation"); setIssueDate(today()); setExpiryDate(""); setReissueOf(""); setIssued(null); } setError(""); }
  function resetForm(next: IssuableType = type) { selectKind(kindOrder[next][0], true); }
  function editReissue(item: CertificateRecord) { const nextKind = kindFromSubject(item.subject, item.type); const cfg = kinds[nextKind]; const [from,to] = serviceDatesFromDescription(item.description || ""); setType(cfg.type); setKind(nextKind); setName(item.name || ""); setAddress(item.address || ""); setFatherName(item.fatherName || ""); setMotherName(item.motherName || ""); setNidNo(item.nidNo || ""); setServiceFrom(from); setServiceTo(to); setPosition(item.position || cfg.position); setSubject(item.subject || cfg.subject); setReference(item.reference || ""); setDescription(stripServicePeriod(item.description || cfg.statement)); setIssueDate(today()); setExpiryDate(localDate(item.expiresAt)); setReissueOf(item.certificateId); setIssued(null); setError(""); window.scrollTo({ top: 0, behavior: "smooth" }); }

  async function issue(e: FormEvent) {
    e.preventDefault(); if (loading) return;
    if (!name.trim() || !address.trim() || !position.trim() || !subject.trim()) { setError("Name, address, position and certificate subject are required."); return; }
    if (kind === "experience" && (!serviceFrom || !serviceTo)) { setError("Service From and Service To dates are required for an Experience Certificate."); return; }
    if (kind === "experience" && serviceFrom > serviceTo) { setError("Service To date cannot be earlier than Service From date."); return; }
    if (type === "project" && [buildingStories, buildingUse, saDag, bsDag, jlNo, mouza, landArea, ps, district, structuralDesigner, soilTestCompany, soilTestDate, constructionProgress].some((value) => !value.trim())) { setError("Complete all project, land, structural design, soil test and construction-stage fields before issuing a Project Certificate."); return; }
    setLoading(true); setError("");
    try {
      const cleanDescription = stripServicePeriod(description);
      const finalDescription = type === "project"
        ? projectCertificateStatement(kind, { name, buildingStories, buildingUse, saDag, bsDag, jlNo, mouza, landArea, ps, district, structuralDesigner, soilTestCompany, soilTestDate, constructionProgress })
        : kind === "experience" ? `Service Period: ${ordinalDate(serviceFrom)} to ${ordinalDate(serviceTo)}\n\n${cleanDescription}` : cleanDescription;
      const response = await fetch("/api/certificates", { method: "POST", headers: { "Content-Type": "application/json" }, credentials: "same-origin", body: JSON.stringify({ type, category: kind, name, address, fatherName, motherName, nidNo, position, subject, reference, description: finalDescription, reissueOf, issuedAt: new Date(`${issueDate}T12:00:00+06:00`).toISOString(), expiresAt: expiryDate ? new Date(`${expiryDate}T23:59:59+06:00`).toISOString() : "" }) });
      const json = await response.json(); if (!response.ok || !json?.success) throw new Error(json?.error || "Could not issue certificate."); setIssued(json.data); setReissueOf(""); await loadRegistry(); setTimeout(() => document.getElementById("certificate-preview")?.scrollIntoView({ behavior: "smooth", block: "start" }), 50);
    } catch (e: any) { setError(e?.message || "Could not issue certificate."); } finally { setLoading(false); }
  }

  async function statusAction(item: CertificateRecord, action: "revoke" | "delete") { if (action === "revoke") { const reason = window.prompt(`Reason for revoking ${item.certificateId}:`, ""); if (reason === null) return; if (!window.confirm(`Revoke ${item.certificateId}? Its QR will show REVOKED.`)) return; await patch(item.certificateId, action, reason); } else { if (!window.confirm(`Withdraw ${item.certificateId}? Its QR will show WITHDRAWN. This does not erase the audit record.`)) return; const reason = window.prompt("Withdrawal reason (optional):", "") ?? ""; await patch(item.certificateId, action, reason); } }
  async function patch(certificateId: string, action: string, reason: string) { try { const response = await fetch("/api/certificates", { method: "PATCH", headers: { "Content-Type": "application/json" }, credentials: "same-origin", body: JSON.stringify({ certificateId, action, reason }) }); const json = await response.json(); if (!response.ok || !json?.success) throw new Error(json?.error || "Could not update certificate."); await loadRegistry(); } catch (e: any) { window.alert(e?.message || "Could not update certificate."); } }

  const stats = useMemo(() => ({ total: records.length, active: records.filter((x) => (x.status || "Active") === "Active").length, superseded: records.filter((x) => x.status === "Superseded").length, inactive: records.filter((x) => x.status === "Revoked" || x.status === "Deleted").length }), [records]);
  const filtered = useMemo(() => { const q = query.trim().toLowerCase(); return records.filter((x) => !q || [x.certificateId, x.type, x.name, x.reference, x.subject, x.status].join(" ").toLowerCase().includes(q)); }, [records, query]);

  return <div className="cert-page">
    <style>{`.cert-page{display:grid;gap:20px;color:#eee}.cert-hero small,.cert-head small,.cert-section-label{color:#ef6e64;font-size:12px;font-weight:900;letter-spacing:.13em}.cert-hero h1{font-size:34px;margin:7px 0 4px}.cert-hero p{margin:0;color:#858b91;font-size:12px}.cert-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.cert-stat,.cert-panel,.cert-registry{border:1px solid #34373b;background:#1b1d1f;border-radius:12px}.cert-stat{padding:15px}.cert-stat span{color:#7d848b;font-size:12px;font-weight:900}.cert-stat strong{display:block;font-size:24px;margin-top:7px}.cert-layout{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:16px}.cert-head{padding:17px 19px;border-bottom:1px solid #303438}.cert-head h2{font-size:17px;margin:6px 0 0}.cert-tabs{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;padding:14px 18px 0}.cert-tabs button,.cert-subtypes button,.cert-actions button,.cert-actions a,.cert-toolbar button{border:1px solid #3a3f44;background:#151719;color:#ddd;border-radius:7px;cursor:pointer;text-decoration:none}.cert-tabs button{height:52px;font-size:13px;font-weight:900}.cert-tabs button.active{border-color:var(--brand-red);background:rgba(239,73,59,.1);color:white}.cert-subtype-wrap{padding:14px 18px 0}.cert-section-label{display:block;margin-bottom:8px}.cert-subtypes{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.cert-subtypes.two{grid-template-columns:repeat(2,minmax(0,1fr))}.cert-subtypes button{min-height:44px;padding:8px 10px;font-size:11px;font-weight:900;line-height:1.25}.cert-subtypes button.active{border-color:#ef6e64;background:#261817;color:#fff}.cert-form{padding:18px}.cert-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.cert-field{display:grid;gap:6px}.cert-field.full{grid-column:1/-1}.cert-field span{color:#8b9298;font-size:12px;font-weight:900;letter-spacing:.08em}.cert-field input,.cert-field textarea,.cert-search{width:100%;border:1px solid #383c40;border-radius:8px;background:#101214;color:#eee;outline:none;padding:0 11px}.cert-field input{height:43px}.cert-field textarea{min-height:146px;padding-top:10px;line-height:1.5}.cert-submit{height:48px;width:100%;border:0;border-radius:8px;background:var(--brand-red);color:#fff;font-size:12px;font-weight:900;margin-top:15px;cursor:pointer}.cert-submit:disabled{opacity:.55}.cert-alert{margin-bottom:12px;padding:10px;border-radius:8px;background:#321c19;color:#f1aaa2;font-size:12px}.cert-reissue{margin-bottom:12px;padding:11px;border:1px solid #645127;border-radius:8px;background:#221f17;color:#dfc67c;font-size:12px}.cert-help{padding:18px}.cert-help article{padding:12px 0;border-bottom:1px solid #303337}.cert-help strong{font-size:12px}.cert-help p{font-size:12px;color:#838a90;line-height:1.55;margin:5px 0 0}.cert-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:14px 16px;border-bottom:1px solid #303438}.cert-search{height:40px;max-width:360px}.cert-toolbar button{height:39px;padding:0 13px;font-size:12px;font-weight:900}.cert-table-wrap{overflow:auto}.cert-table{width:100%;border-collapse:collapse;min-width:900px}.cert-table th,.cert-table td{padding:11px 13px;text-align:left;border-bottom:1px solid #2e3236;font-size:12px}.cert-table th{color:#777f86;font-size:12px}.cert-status{display:inline-flex;padding:5px 7px;border-radius:999px;font-size:12px;font-weight:900;background:#233026;color:#9dd2ab}.cert-status.superseded{background:#332d1c;color:#dcc276}.cert-status.revoked,.cert-status.deleted{background:#3b211e;color:#ee9e94}.cert-actions{display:flex;gap:5px;flex-wrap:wrap}.cert-actions button,.cert-actions a{padding:6px 8px;font-size:12px;font-weight:900}.cert-actions .danger{border-color:#5c302a;color:#ef998f}.cert-actions button:disabled{opacity:.4;cursor:not-allowed}.certificate-wrap{scroll-margin-top:120px}.certificate-actions{display:flex;justify-content:flex-end;gap:8px;margin-bottom:10px}.certificate-actions a,.certificate-actions button{height:38px;padding:0 13px;border:1px solid #3a3e42;border-radius:7px;background:#202326;color:#ddd;text-decoration:none;display:inline-flex;align-items:center;font-size:12px;font-weight:900}.cert-empty{padding:20px;color:#888}@media(max-width:950px){.cert-layout{grid-template-columns:1fr}.cert-stats{grid-template-columns:1fr 1fr}}@media(max-width:620px){.cert-grid{grid-template-columns:1fr}.cert-field.full{grid-column:auto}.cert-tabs,.cert-subtypes,.cert-subtypes.two{grid-template-columns:1fr}}`}</style>
    <header className="cert-hero"><small>OFFICIAL DOCUMENTS</small><h1>Certificate center.</h1><p>Choose Project Certificate or Employee Certificate, then select the exact certificate you want to issue.</p></header>
    <section className="cert-stats"><article className="cert-stat"><span>TOTAL REGISTRY</span><strong>{stats.total}</strong></article><article className="cert-stat"><span>ACTIVE</span><strong>{stats.active}</strong></article><article className="cert-stat"><span>SUPERSEDED</span><strong>{stats.superseded}</strong></article><article className="cert-stat"><span>REVOKED / WITHDRAWN</span><strong>{stats.inactive}</strong></article></section>
    <section className="cert-layout"><div className="cert-panel"><div className="cert-head"><small>{reissueOf ? "REVISION MODE" : "CERTIFICATE ISSUANCE"}</small><h2>{reissueOf ? `Reissue ${reissueOf}` : kinds[kind].label}</h2></div>
      <div className="cert-tabs">{(["project", "employee"] as IssuableType[]).map((item) => <button key={item} type="button" className={type === item ? "active" : ""} onClick={() => resetForm(item)}>{item === "project" ? "Project Certificate" : "Employee Certificate"}</button>)}</div>
      <div className="cert-subtype-wrap"><span className="cert-section-label">{type === "project" ? "PROJECT CERTIFICATES" : "EMPLOYEE CERTIFICATES"}</span><div className={`cert-subtypes ${type === "employee" ? "two" : ""}`}>{kindOrder[type].map((item) => <button key={item} type="button" className={kind === item ? "active" : ""} onClick={() => selectKind(item)}>{kinds[item].label}</button>)}</div></div>
      <form className="cert-form" onSubmit={issue}>{reissueOf && <div className="cert-reissue">Editing an issued certificate creates a new revision. The old QR remains traceable and shows SUPERSEDED.</div>}{error && <div className="cert-alert">{error}</div>}<div className="cert-grid">
        <label className="cert-field"><span>{type === "employee" ? "EMPLOYEE NAME" : "NAME / OWNER / CLIENT"}</span><input value={name} onChange={(e) => setName(e.target.value)} /></label><label className="cert-field"><span>POSITION / DESIGNATION</span><input value={position} onChange={(e) => setPosition(e.target.value)} /></label><label className="cert-field full"><span>ADDRESS</span><input value={address} onChange={(e) => setAddress(e.target.value)} /></label>
        {type === "employee" && <><label className="cert-field"><span>FATHER'S NAME</span><input value={fatherName} onChange={(e) => setFatherName(e.target.value)} /></label><label className="cert-field"><span>MOTHER'S NAME</span><input value={motherName} onChange={(e) => setMotherName(e.target.value)} /></label><label className="cert-field full"><span>NID NO.</span><input inputMode="numeric" value={nidNo} onChange={(e) => setNidNo(e.target.value)} /></label>{kind === "experience" && <><label className="cert-field"><span>SERVICE FROM</span><input type="date" value={serviceFrom} onChange={(e) => setServiceFrom(e.target.value)} required /></label><label className="cert-field"><span>SERVICE TO</span><input type="date" value={serviceTo} onChange={(e) => setServiceTo(e.target.value)} required /></label></>}</>}
        {type === "project" && <><label className="cert-field"><span>BUILDING STORIES</span><input value={buildingStories} onChange={(e) => setBuildingStories(e.target.value)} placeholder="07 (Seven)" /></label><label className="cert-field"><span>BUILDING USE</span><input value={buildingUse} onChange={(e) => setBuildingUse(e.target.value)} placeholder="Residential" /></label><label className="cert-field"><span>S.A. DAG NO.</span><input value={saDag} onChange={(e) => setSaDag(e.target.value)} placeholder="3887, 3888, 3889" /></label><label className="cert-field"><span>B.S. DAG NO.</span><input value={bsDag} onChange={(e) => setBsDag(e.target.value)} placeholder="10058" /></label><label className="cert-field"><span>J.L. NO.</span><input value={jlNo} onChange={(e) => setJlNo(e.target.value)} placeholder="42" /></label><label className="cert-field"><span>MOUZA</span><input value={mouza} onChange={(e) => setMouza(e.target.value)} placeholder="Rampur" /></label><label className="cert-field"><span>LAND AREA (DECIMAL)</span><input value={landArea} onChange={(e) => setLandArea(e.target.value)} placeholder="4.65" /></label><label className="cert-field"><span>P.S. / UPAZILA</span><input value={ps} onChange={(e) => setPs(e.target.value)} placeholder="Feni Sadar" /></label><label className="cert-field"><span>DISTRICT</span><input value={district} onChange={(e) => setDistrict(e.target.value)} /></label><label className="cert-field"><span>STRUCTURAL DESIGNER</span><input value={structuralDesigner} onChange={(e) => setStructuralDesigner(e.target.value)} placeholder="Md. Ali Akbar Siam" /></label><label className="cert-field"><span>SOIL TEST COMPANY</span><input value={soilTestCompany} onChange={(e) => setSoilTestCompany(e.target.value)} placeholder="UNIVERSAL ENGINEERS" /></label><label className="cert-field"><span>SOIL TEST REPORT DATE</span><input value={soilTestDate} onChange={(e) => setSoilTestDate(e.target.value)} placeholder="September 2022" /></label><label className="cert-field full"><span>CONSTRUCTION STAGE / PROGRESS</span><input value={constructionProgress} onChange={(e) => setConstructionProgress(e.target.value)} placeholder="ground floor casting including the foundation" /></label></>}
        <label className="cert-field full"><span>CERTIFICATE SUBJECT</span><input value={subject} onChange={(e) => setSubject(e.target.value)} /></label><label className="cert-field"><span>REFERENCE / FILE ID</span><input value={reference} onChange={(e) => setReference(e.target.value)} /></label><label className="cert-field"><span>ISSUE DATE</span><input type="date" value={issueDate} onChange={(e) => setIssueDate(e.target.value)} /></label><label className="cert-field"><span>EXPIRY DATE (OPTIONAL)</span><input type="date" value={expiryDate} onChange={(e) => setExpiryDate(e.target.value)} /></label><label className="cert-field full"><span>CERTIFICATE STATEMENT</span><textarea maxLength={3000} value={description} onChange={(e) => setDescription(e.target.value)} /></label>
      </div><button className="cert-submit" disabled={loading}>{loading ? "SAVING…" : reissueOf ? "ISSUE NEW REVISION" : `ISSUE ${kinds[kind].label.toUpperCase()} & CREATE QR`}</button></form></div>
      <aside className="cert-panel"><div className="cert-head"><small>CERTIFICATE STRUCTURE</small><h2>Available certificates</h2></div><div className="cert-help"><article><strong>Project Certificate</strong><p>Load Bearing Certificate, Construction Supervision Certificate, and Construction Completion Certificate.</p></article><article><strong>Employee Certificate</strong><p>Experience Certificate and Salary Certificate.</p></article><article><strong>Experience service period</strong><p>Service From and Service To appear only for Experience Certificates and print automatically in ordinal date format.</p></article><article><strong>Edit = Reissue</strong><p>Issued content is never overwritten. Editing creates a new revision and marks the old certificate Superseded.</p></article><article><strong>Revoke / Withdraw</strong><p>Certificate history remains auditable and the verification result shows the current status.</p></article></div></aside></section>
    {issued && <section className="certificate-wrap" id="certificate-preview"><div className="certificate-actions"><a href={issued.verificationUrl} target="_blank" rel="noreferrer">VERIFY ONLINE</a><button onClick={() => void printCertificate().catch((error: Error) => window.alert(error.message))}>PRINT / SAVE PDF</button></div><CertificateDocument issued={issued} /></section>}
    <section className="cert-registry"><div className="cert-head"><small>CENTRAL REGISTRY</small><h2>Issued certificates</h2></div><div className="cert-toolbar"><input className="cert-search" placeholder="Search ID, name, certificate, status…" value={query} onChange={(e) => setQuery(e.target.value)} /><button onClick={loadRegistry} disabled={loadingRegistry}>{loadingRegistry ? "REFRESHING…" : "REFRESH"}</button></div>{registryError && <div className="cert-alert" style={{ margin: 14 }}>{registryError}</div>}{!loadingRegistry && !filtered.length ? <div className="cert-empty">No certificates in the central registry yet.</div> : <div className="cert-table-wrap"><table className="cert-table"><thead><tr><th>Certificate</th><th>Name / Subject</th><th>Reference</th><th>Revision</th><th>Status</th><th>Issued</th><th>Actions</th></tr></thead><tbody>{filtered.map((item) => { const status = item.status || "Active"; const disabled = status === "Deleted" || status === "Revoked" || status === "Superseded"; return <tr key={item.certificateId}><td><strong>{item.certificateId}</strong><br/>{item.subject || legacyMeta[item.type]?.label || item.type}</td><td><strong>{item.name}</strong><br/>{item.subject}</td><td>{item.reference || "—"}</td><td>R{item.revision || 1}</td><td><span className={`cert-status ${status.toLowerCase()}`}>{status.toUpperCase()}</span>{item.supersededBy ? <><br/>→ {item.supersededBy}</> : null}</td><td>{displayDate(item.issuedAt)}</td><td><div className="cert-actions">{item.verificationUrl && <a href={item.verificationUrl} target="_blank" rel="noreferrer">VERIFY</a>}<button onClick={() => { setIssued(item); setTimeout(() => document.getElementById("certificate-preview")?.scrollIntoView({ behavior: "smooth", block: "start" }), 50); }}>PREVIEW / PRINT</button><button onClick={() => editReissue(item)} disabled={status === "Deleted" || item.type === "building"}>EDIT / REISSUE</button><button onClick={() => statusAction(item, "revoke")} disabled={disabled}>REVOKE</button><button className="danger" onClick={() => statusAction(item, "delete")} disabled={status === "Deleted"}>WITHDRAW</button></div></td></tr>; })}</tbody></table></div>}</section>
  </div>;
}
