name: Patch Code.gs access controls

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/patch-code-gs.yml
      - scripts/patch_chairman_approval.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch chairman approval routes into Code.gs
        run: python3 scripts/patch_chairman_approval.py
      - name: Commit Code.gs
        run: |
          if git diff --quiet -- Code.gs; then
            echo "No Code.gs changes required"
            exit 0
          fi
          git config user.name "LAND VIEW Automation"
          git config user.email "actions@users.noreply.github.com"
          git add Code.gs
          git commit -m "Add chairman pending expense approval routes"
          git push origin HEAD:main
