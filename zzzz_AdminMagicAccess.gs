/* LAND VIEW — OWNER MAGIC ACCESS RETIRED
 *
 * The permanent bearer-link admin bridge has been removed. Authentication is
 * provided by zzz_SessionSecurityV2.gs, which supports the normal LAND VIEW
 * admin / manager / accounts / employee / client password flow with session
 * expiry, password hashing, login throttling and audit logging.
 *
 * Intentionally no loginUser() override is defined in this file.
 */
