# LAND VIEW Google Apps Script Web App URL.
# Use the deployed /exec URL, never the /dev URL.
# This is server-side only and should be configured in Vercel as LAND_VIEW_API_URL.
LAND_VIEW_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
